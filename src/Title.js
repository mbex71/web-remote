import React from 'react'


const Title = (props) =>{
    // const [user, setUser] = useState(null)
    return (
        <div>
        Web Remote {props.title} 
        </div>
        
    )
}

export default Title